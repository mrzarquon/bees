#!/bin/bash
 
PE_MASTER='foobar'

mkdir -p /etc/puppetlabs/facter/facts.d/

echo "ec2_instance_id=$(curl -s http://169.254.169.254/latest/meta-data/instance-id)" > /etc/puppetlabs/facter/facts.d/ec2.txt

curl -sk https://${PE_MASTER}:8140/packages/current/install.bash | sudo /bin/bash

